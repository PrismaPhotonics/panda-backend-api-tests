# בדיקת מרקרים חסרים

**תאריך:** 2025-11-19  
**מטרה:** לבדוק אם יש בדיקות שצריכות מרקרים אבל חסרות

---

## ✅ בדיקות שצריכות `@pytest.mark.nightly`

### בדיקות עם `@pytest.mark.slow` שלא מסומנות כ-`nightly`:

1. ✅ `test_deep_alert_logs_investigation.py` - **חסר `@pytest.mark.nightly`**
   - יש `@pytest.mark.slow`
   - יש `@pytest.mark.regression`
   - **צריך להוסיף:** `@pytest.mark.nightly`

---

## ✅ בדיקות שצריכות `@pytest.mark.high`

כל הבדיקות עם `@pytest.mark.critical` כבר מסומנות עם `@pytest.mark.high` ✅

---

## ✅ בדיקות שצריכות `@pytest.mark.smoke`

הבדיקות הבסיסיות כבר מסומנות עם `@pytest.mark.smoke` ✅

---

## 📝 סיכום

**חסר מרקר אחד:**
- `test_deep_alert_logs_investigation.py` - צריך להוסיף `@pytest.mark.nightly`

**הערה:** המשתמש הסיר את `@pytest.mark.nightly` מהקובץ הזה, אבל זה בדיקה איטית (`@pytest.mark.slow`) ולכן היא צריכה להיות חלק מה-nightly suite.

---

**עודכן לאחרונה:** 2025-11-19


# 🚀 הוראות הרצה מהירות - טסטים חדשים

**עודכן:** 27 אוקטובר 2025

---

## ⚡ Quick Start - התחלה מהירה (5 דקות)

### 1. הרץ את הטסט הכי חשוב (200 concurrent jobs):

```powershell
cd c:\Projects\focus_server_automation

pytest tests/load/test_job_capacity_limits.py::Test200ConcurrentJobsCapacity::test_200_concurrent_jobs_target_capacity -v -s
```

**מה יקרה:**
- יווצרו 200 jobs concurrent
- יימדד success rate
- אם סביבה לא עומדת ביעד → דוח מפורט יווצר

**זמן משוער:** 3-5 דקות

---

### 2. בדוק אם נוצר Infrastructure Gap Report:

```powershell
dir reports\infra_gap_report_*.json
```

**אם נוצר דוח:**
1. פתח את הקובץ JSON
2. חפש: `"bottleneck_analysis"`
3. קרא: `"recommendations.immediate_actions"`
4. שתף עם DevOps

---

## 📋 כל הטסטים החדשים - רשימה מלאה

### A. טסטי Infrastructure (10 טסטים):

```bash
# K8s Job Lifecycle (5 tests)
pytest tests/infrastructure/test_k8s_job_lifecycle.py -v -s

# System Behavior (5 tests, ללא 1-hour)
pytest tests/infrastructure/test_system_behavior.py -v -s -m "not slow"
```

---

### B. טסטי Integration/API (10 טסטים):

```bash
# Pre-Launch Validations (10 tests)
pytest tests/integration/api/test_prelaunch_validations.py -v -s
```

---

### C. טסטי Load (1 טסט):

```bash
# 200 Concurrent Jobs
pytest tests/load/test_job_capacity_limits.py::Test200ConcurrentJobsCapacity -v -s
```

---

## 🎯 הרצה לפי מטרה

### מטרה 1: "רק רוצה לראות שהכל עובד" (Smoke Test)

```bash
# הרץ טסט אחד מכל קטגוריה
pytest tests/infrastructure/test_system_behavior.py::TestFocusServerCleanStartup::test_focus_server_clean_startup -v -s

pytest tests/integration/api/test_prelaunch_validations.py::TestPortAvailabilityValidation::test_port_availability_before_job_creation -v -s

pytest tests/infrastructure/test_k8s_job_lifecycle.py::TestK8sJobCreation::test_k8s_job_creation_triggers_pod_spawn -v -s
```

**זמן:** ~3 דקות

---

### מטרה 2: "בדיקת capacity מלאה" (הכי חשוב!)

```bash
pytest tests/load/test_job_capacity_limits.py::Test200ConcurrentJobsCapacity -v -s
```

**זמן:** ~5 דקות  
**קריטי:** זה הדרישה העיקרית מהפגישה!

---

### מטרה 3: "הרצה מלאה של כל החדש" (Regression)

```bash
# כל הטסטים החדשים (ללא slow)
pytest tests/infrastructure/test_k8s_job_lifecycle.py -v -s
pytest tests/integration/api/test_prelaunch_validations.py -v -s
pytest tests/infrastructure/test_system_behavior.py -v -s -m "not slow"
pytest tests/load/test_job_capacity_limits.py::Test200ConcurrentJobsCapacity -v -s
```

**זמן:** ~15-20 דקות

---

## ⚠️ בעיות אפשריות ופתרונות

### בעיה: "pytest: command not found"

**פתרון:**
```powershell
# אפשרות 1: השתמש ב-python -m
python -m pytest tests/load/test_job_capacity_limits.py -v -s

# אפשרות 2: הפעל virtual environment
.venv\Scripts\activate
pytest tests/load/test_job_capacity_limits.py -v -s
```

---

### בעיה: "No module named 'src'"

**פתרון:**
```powershell
# התקן את הפרויקט
pip install -e .

# או הוסף ל-PYTHONPATH
$env:PYTHONPATH = "c:\Projects\focus_server_automation"
```

---

### בעיה: "Kubernetes not available"

**פתרון:**
טסטי K8s ידלגו אוטומטית אם K8s לא זמין.

אם רוצה להריץ בכל זאת:
```powershell
# ודא ש-kubectl מוגדר
kubectl get pods

# ודא שyou're connected לcluster
kubectl config current-context
```

---

## 📊 פרשנות תוצאות

### תוצאה: ✅ כל הטסטים עברו

**משמעות:**
- ✅ סביבה תומכת ב-200 concurrent jobs
- ✅ Pre-launch validations עובדים
- ✅ K8s orchestration תקין
- ✅ System behavior צפוי

**מה הלאה:**
- שלב ב-CI/CD
- קשר לXray
- הרץ regression weekly

---

### תוצאה: ❌ טסט 200 jobs נכשל

**משמעות:**
- ❌ סביבה לא תומכת ב-200 jobs
- 📊 Infrastructure Gap Report נוצר

**מה לעשות:**
1. פתח: `reports/infra_gap_report_*.json`
2. קרא את הBottlenecks
3. עקוב אחר Recommendations:
   - "Scale Kubernetes cluster"
   - "Increase pod resources"
   - וכו'...
4. שתף עם DevOps
5. תקן
6. הרץ שוב

---

### תוצאה: ⚠️ חלק מהטסטים נכשלו

**מה לעשות:**
1. בדוק איזה טסט נכשל
2. קרא את הlogs (pytest מציג logs מפורטים)
3. זהה את הבעיה
4. תקן
5. הרץ שוב

---

## 📚 מסמכים לקריאה

### חייב לקרוא (5 דקות):
- **[סיכום_עדכון_טסטים_PZ-13756.md](סיכום_עדכון_טסטים_PZ-13756.md)** - מה נעשה בקצרה
- **[EXECUTIVE_SUMMARY_HE.md](documentation/meetings/EXECUTIVE_SUMMARY_HE.md)** - סיכום מנהלים

### מומלץ לקרוא (15 דקות):
- **[SCOPE_REFINEMENT_INDEX.md](documentation/meetings/SCOPE_REFINEMENT_INDEX.md)** - אינדקס לכל המסמכים
- **README files** בתיקיות הטסטים

### אופציונלי (30+ דקות):
- **[SCOPE_REFINEMENT_ACTION_PLAN.md](documentation/meetings/SCOPE_REFINEMENT_ACTION_PLAN.md)** - תוכנית מלאה
- **[SCOPE_REFINEMENT_COMPLETION_SUMMARY.md](documentation/meetings/SCOPE_REFINEMENT_COMPLETION_SUMMARY.md)** - סיכום טכני

---

## 🎯 Next Steps - 3 צעדים הבאים

### היום (27 אוקטובר):
1. [ ] קרא את הסיכום (5 דקות)
2. [ ] הרץ טסט 200 jobs (5 דקות)
3. [ ] סקור תוצאות

### מחר (28 אוקטובר):
4. [ ] הרץ כל הטסטים החדשים (20 דקות)
5. [ ] תקן בעיות (אם יש)
6. [ ] שתף תוצאות עם צוות

### השבוע:
7. [ ] הרץ בכל הסביבות
8. [ ] אינטגרציה עם CI/CD
9. [ ] צור Jira ticket לbacklog

---

## ✅ Checklist אישי

- [ ] קראתי את הסיכום
- [ ] הבנתי מה שונה
- [ ] ידעתי איך להריץ את הטסטים
- [ ] הרצתי לפחות טסט אחד
- [ ] יודע מה לעשות עם Gap Report
- [ ] יודע מה הצעדים הבאים

---

**🎉 זהו! הכל מוכן. תתחיל להריץ!**

**הפקודה הראשונה:**
```bash
pytest tests/load/test_job_capacity_limits.py::Test200ConcurrentJobsCapacity -v -s
```

**בהצלחה! 🚀**


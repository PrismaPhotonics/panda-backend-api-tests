name: Run Tests (Excluding Load Tests)

on:
  push:
    branches: [ main, develop, master ]
  pull_request:
    branches: [ main, develop, master ]
  workflow_dispatch:

env:
  PYTHON_VERSION: '3.10'

jobs:
  backend-tests:
    name: Backend Tests (No Load)
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: ${{ env.PYTHON_VERSION }}
        cache: 'pip'

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential

    - name: Install Python dependencies
      run: |
        # Upgrade pip, setuptools, and wheel to latest versions
        python -m pip install --upgrade pip setuptools wheel
        # Install dependencies in stages to avoid resolution conflicts
        # Stage 1: Core pytest packages
        pip install --no-cache-dir --prefer-binary \
          pytest==8.2.0 \
          pytest-asyncio==0.23.0 \
          pytest-timeout==2.3.0 \
          pytest-mock==3.14.0 \
          pytest-html==4.1.1 \
          pytest-cov==5.0.0 \
          pytest-json-report==1.5.0 \
          pytest-xdist==3.6.0
        # Stage 2: HTTP and data processing
        pip install --no-cache-dir --prefer-binary \
          requests==2.32.3 \
          httpx==0.27.0 \
          beautifulsoup4==4.12.3 \
          pydantic==2.9.0 \
          pydantic-settings==2.5.0 \
          orjson==3.10.0 \
          pyyaml==6.0.1
        # Stage 3: Infrastructure and utilities
        pip install --no-cache-dir --prefer-binary \
          kubernetes==30.1.0 \
          pymongo==4.10.0 \
          paramiko==3.5.0 \
          pika==1.3.2 \
          structlog==24.4.0 \
          colorlog==6.8.2 \
          python-dateutil==2.9.0 \
          pytz==2024.1 \
          psutil==5.9.8 \
          python-dotenv==1.0.1 \
          netaddr==0.10.1 \
          jira==3.10.5
        # Stage 4: Browser automation (problematic packages)
        pip install --no-cache-dir --prefer-binary \
          playwright==1.48.0 \
          pytest-playwright==0.4.3 || true
        # Stage 5: Reporting and optional packages
        pip install --no-cache-dir --prefer-binary \
          allure-pytest==2.13.2 \
          jinja2==3.1.4 \
          asyncio-mqtt==0.16.2 \
          aiofiles==24.1.0 \
          pytest-xray==0.2.1 || true
        # Stage 6: Development tools (optional, can fail)
        pip install --no-cache-dir --prefer-binary \
          black==24.8.0 \
          flake8==7.1.1 \
          mypy==1.11.0 \
          isort==5.13.2 \
          cryptography==43.0.0 \
          bandit==1.7.8 \
          sphinx==8.1.3 \
          sphinx-rtd-theme==3.0.2 || true

    - name: Create reports directory
      run: |
        mkdir -p reports

    - name: Check Focus Server availability
      id: check_focus
      env:
        FOCUS_BASE_URL: ${{ secrets.FOCUS_BASE_URL }}
        FOCUS_API_PREFIX: ${{ secrets.FOCUS_API_PREFIX || '/focus-server' }}
      run: |
        if [ -z "$FOCUS_BASE_URL" ]; then
          echo "available=false" >> $GITHUB_OUTPUT
          echo "⚠️ FOCUS_BASE_URL secret not configured - tests requiring Focus Server will be skipped"
          echo "ℹ️  To enable Focus Server tests, configure FOCUS_BASE_URL secret in repository settings"
        else
          BASE="${FOCUS_BASE_URL%/}${FOCUS_API_PREFIX}"
          echo "Checking Focus Server at $BASE/ack (health check endpoint)"
          set +e
          # Try /ack endpoint first (faster than /health)
          code=$(curl -sk -o /dev/null -w "%{http_code}" --connect-timeout 5 --max-time 10 "$BASE/ack" 2>/dev/null || echo "000")
          if [[ "$code" -ge 200 && "$code" -lt 500 ]]; then
            echo "available=true" >> $GITHUB_OUTPUT
            echo "✅ Focus Server is reachable (HTTP $code)"
            # Also check channels endpoint
            channels_code=$(curl -sk -o /dev/null -w "%{http_code}" --connect-timeout 5 --max-time 10 "$BASE/channels" 2>/dev/null || echo "000")
            if [[ "$channels_code" -ge 200 && "$channels_code" -lt 500 ]]; then
              echo "✅ Focus Server channels endpoint is also accessible"
            fi
          else
            echo "available=false" >> $GITHUB_OUTPUT
            echo "⚠️ Focus Server not reachable (HTTP $code) - tests requiring Focus Server will be skipped"
            echo "ℹ️  This is expected in CI if Focus Server is behind VPN/firewall"
          fi
        fi

    - name: Run All Tests (Excluding Load/Stress)
      env:
        CI: true
        # Use local environment to skip auto infrastructure setup (SSH port-forwards)
        FOCUS_ENV: local
        # Allow Focus Server URL override via secrets (if available)
        FOCUS_BASE_URL: ${{ secrets.FOCUS_BASE_URL }}
        FOCUS_API_PREFIX: ${{ secrets.FOCUS_API_PREFIX || '/focus-server' }}
        VERIFY_SSL: ${{ secrets.VERIFY_SSL || 'false' }}
        # Set environment variable to indicate if Focus Server is available
        FOCUS_SERVER_AVAILABLE: ${{ steps.check_focus.outputs.available }}
        # Don't set SKIP_INFRASTRUCTURE_TESTS - let fixtures handle skipping gracefully
      run: |
        # Override testpaths from pytest.ini to avoid conflicts
        pytest be_focus_server_tests/ \
          --env=local \
          --skip-health-check \
          --skip-sanity-check \
          --override-ini="testpaths=" \
          --ignore=be_focus_server_tests/load \
          --ignore=be_focus_server_tests/stress \
          --ignore-glob=**/integration/load \
          --ignore-glob=**/test_alert_generation_load.py \
          --ignore-glob=**/test_alert_generation_performance.py \
          -m "not load and not stress and not grpc" \
          -v \
          --tb=short \
          --junitxml=reports/junit-backend.xml \
          --html=reports/backend-report.html \
          --self-contained-html \
          --maxfail=10 \
          --continue-on-collection-errors

    - name: Upload test reports
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: backend-test-reports
        path: |
          reports/junit-backend.xml
          reports/backend-report.html
        retention-days: 30

  frontend-tests:
    name: Frontend Tests
    runs-on: ubuntu-latest
    timeout-minutes: 30
    continue-on-error: true

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Check if frontend tests exist
      id: check_frontend
      run: |
        if [ -d "fe_panda_tests" ] && [ -f "fe_panda_tests/requirements.txt" ]; then
          echo "exists=true" >> $GITHUB_OUTPUT
        else
          echo "exists=false" >> $GITHUB_OUTPUT
        fi

    - name: Set up Python
      if: steps.check_frontend.outputs.exists == 'true'
      uses: actions/setup-python@v5
      with:
        python-version: ${{ env.PYTHON_VERSION }}
        cache: 'pip'

    - name: Install Python dependencies
      if: steps.check_frontend.outputs.exists == 'true'
      run: |
        python -m pip install --upgrade pip
        pip install -r fe_panda_tests/requirements.txt

    - name: Create reports directory
      if: steps.check_frontend.outputs.exists == 'true'
      run: |
        mkdir -p reports

    - name: Run Frontend Tests
      if: steps.check_frontend.outputs.exists == 'true'
      run: |
        cd fe_panda_tests
        pytest tests/ \
          -v \
          --tb=short \
          --junitxml=../reports/junit-frontend.xml \
          --html=../reports/frontend-report.html \
          --self-contained-html \
          --maxfail=10 || true

    - name: Skip frontend tests message
      if: steps.check_frontend.outputs.exists != 'true'
      run: |
        echo "⚠️ Frontend tests directory not found - skipping frontend tests"

    - name: Upload frontend test reports
      if: always() && steps.check_frontend.outputs.exists == 'true'
      uses: actions/upload-artifact@v4
      with:
        name: frontend-test-reports
        path: |
          reports/junit-frontend.xml
          reports/frontend-report.html
        retention-days: 30

  all-tests-summary:
    name: All Tests Summary
    runs-on: ubuntu-latest
    needs: [backend-tests]
    if: always()

    steps:
    - name: Download all test reports
      uses: actions/download-artifact@v4

    - name: Create test summary
      run: |
        echo "## Test Results Summary" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "✅ All tests completed (excluding load/stress tests)" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### Excluded Test Categories:" >> $GITHUB_STEP_SUMMARY
        echo "- ❌ Load tests (\`be_focus_server_tests/load/\`)" >> $GITHUB_STEP_SUMMARY
        echo "- ❌ Stress tests (\`be_focus_server_tests/stress/\`)" >> $GITHUB_STEP_SUMMARY
        echo "- ❌ Infrastructure tests (\`be_focus_server_tests/infrastructure/\` - require SSH/K8s/RabbitMQ access)" >> $GITHUB_STEP_SUMMARY
        echo "- ❌ Integration load tests (\`be_focus_server_tests/integration/load/\`)" >> $GITHUB_STEP_SUMMARY
        echo "- ❌ API load tests (\`focus_server_api_load_tests/\`)" >> $GITHUB_STEP_SUMMARY
        echo "- ❌ Alert load/performance tests" >> $GITHUB_STEP_SUMMARY
        echo "- ❌ gRPC stream tests (marked with \`@pytest.mark.grpc\`)" >> $GITHUB_STEP_SUMMARY
        echo "- ❌ Tests requiring infrastructure (MongoDB, RabbitMQ, Kubernetes, SSH)" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### Included Test Categories:" >> $GITHUB_STEP_SUMMARY
        echo "- ✅ Unit tests" >> $GITHUB_STEP_SUMMARY
        echo "- ✅ Integration tests (non-load)" >> $GITHUB_STEP_SUMMARY
        echo "- ✅ Infrastructure tests (will skip if no connection available)" >> $GITHUB_STEP_SUMMARY
        echo "- ✅ Data quality tests" >> $GITHUB_STEP_SUMMARY
        echo "- ✅ Security tests" >> $GITHUB_STEP_SUMMARY
        echo "- ✅ Performance tests (non-load)" >> $GITHUB_STEP_SUMMARY
        echo "- ✅ UI tests" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Note:** Tests run with \`--env=local\` to skip auto infrastructure setup. Tests requiring Focus Server API will attempt connection if \`FOCUS_BASE_URL\` secret is configured, otherwise they will be skipped." >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "📊 Check the artifacts for detailed test reports." >> $GITHUB_STEP_SUMMARY


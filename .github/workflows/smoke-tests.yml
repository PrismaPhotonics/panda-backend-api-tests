name: Smoke Tests

on:
  push:
    branches: [ main, develop, master, chore/add-roy-tests ]
    paths:
      - "be_focus_server_tests/**"
      - "src/**"
      - "config/**"
      - "requirements*.txt"
      - ".github/workflows/**"
  pull_request:
    branches: [ main ]
  workflow_dispatch: {}

jobs:
  smoke-tests:
    name: Smoke Tests
    runs-on: [self-hosted, Windows, X64]
    timeout-minutes: 15

    env:
      PYTHONUNBUFFERED: "1"
      PYTHONIOENCODING: "utf-8"
      ENV_NAME: "staging"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        shell: powershell
        run: |
          python -m pip install --upgrade pip setuptools wheel
          pip install --no-cache-dir --use-deprecated=legacy-resolver -r requirements.txt

      - name: Create reports directory
        shell: powershell
        run: |
          New-Item -ItemType Directory -Force -Path reports | Out-Null

      - name: Run smoke tests
        shell: powershell
        run: |
          pytest be_focus_server_tests/ -m "smoke" -v `
            --junitxml=reports/junit-smoke.xml `
            --html=reports/smoke-report.html `
            --self-contained-html `
            --json-report `
            --json-report-file=reports/smoke-report.json `
            --maxfail=10 `
            --env=$env:ENV_NAME `
            --skip-health-check

      - name: Generate GitHub Actions summary
        if: always()
        shell: powershell
        run: |
          $summaryPath = $env:GITHUB_STEP_SUMMARY
          if (-not $summaryPath) {
            Write-Host "GITHUB_STEP_SUMMARY not set, skipping summary generation"
            exit 0
          }
          
          if (Test-Path "reports/junit-smoke.xml") {
            $xml = [xml](Get-Content "reports/junit-smoke.xml")
            
            # Debug: Log XML structure
            Write-Host "XML Root Element: $($xml.DocumentElement.Name)"
            Write-Host "XML Root Attributes: $($xml.DocumentElement.Attributes | ForEach-Object { $_.Name + '=' + $_.Value })"
            
            # Handle both testsuites (root) and testsuite (single suite) structures
            $suite = $null
            if ($xml.testsuites) {
              Write-Host "Found testsuites structure"
              # Multiple suites - aggregate all
              $total = 0
              $failures = 0
              $errors = 0
              $skipped = 0
              $time = 0.0
              foreach ($s in $xml.testsuites.testsuite) {
                $total += [int]$s.tests
                $failures += [int]$s.failures
                $errors += [int]$s.errors
                $skipped += [int]$s.skipped
                $time += [double]$s.time
              }
            } elseif ($xml.testsuite) {
              Write-Host "Found testsuite structure"
              # Single suite
              $suite = $xml.testsuite
              $total = [int]$suite.tests
              $failures = [int]$suite.failures
              $errors = [int]$suite.errors
              $skipped = [int]$suite.skipped
              $time = [double]$suite.time
            } else {
              Write-Host "Using root element attributes"
              # Try root element attributes
              $total = [int]$xml.DocumentElement.GetAttribute("tests")
              $failures = [int]$xml.DocumentElement.GetAttribute("failures")
              $errors = [int]$xml.DocumentElement.GetAttribute("errors")
              $skipped = [int]$xml.DocumentElement.GetAttribute("skipped")
              $time = [double]$xml.DocumentElement.GetAttribute("time")
            }
            
            Write-Host "Parsed values: total=$total, failures=$failures, errors=$errors, skipped=$skipped, time=$time"
            
            $passed = $total - $failures - $errors - $skipped
            $timeRounded = [math]::Round($time, 2)
            
            $status = if ($failures -eq 0 -and $errors -eq 0) { '[OK]' } else { '[FAIL]' }
            
            $summary = "## Smoke Tests Results $status`n`n"
            $summary += "| Metric | Value |`n"
            $summary += "|--------|-------|`n"
            $summary += "| **Total Tests** | $total |`n"
            $summary += "| **Passed** | $passed |`n"
            $summary += "| **Failed** | $failures |`n"
            $summary += "| **Errors** | $errors |`n"
            $summary += "| **Skipped** | $skipped |`n"
            $summary += "| **Duration** | ${timeRounded}s |`n`n"
            $summary += "### Test Reports`n`n"
            
            # Build artifact download links
            $repo = $env:GITHUB_REPOSITORY
            $runId = $env:GITHUB_RUN_ID
            $serverUrl = $env:GITHUB_SERVER_URL
            if ($repo -and $runId -and $serverUrl) {
              $artifactBaseUrl = "$serverUrl/$repo/actions/runs/$runId"
              $summary += "- [HTML Report]($artifactBaseUrl/artifacts) - Visual test results (download smoke-test-reports artifact)`n"
              $summary += "- [JSON Report]($artifactBaseUrl/artifacts) - Machine-readable data (download smoke-test-reports artifact)`n"
              $summary += "- [JUnit XML]($artifactBaseUrl/artifacts) - CI/CD integration (download smoke-test-reports artifact)`n"
              $summary += "`n**Note:** Download the `smoke-test-reports` artifact to access all report files.`n`n"
            } else {
              $summary += "- HTML Report - Visual test results (check artifacts section)`n"
              $summary += "- JSON Report - Machine-readable data (check artifacts section)`n"
              $summary += "- JUnit XML - CI/CD integration (check artifacts section)`n`n"
            }
            
            if ($failures -gt 0 -or $errors -gt 0) {
              $summary += "### Failed Tests`n`n"
              $failedTests = $xml.SelectNodes("//testcase[failure or error]")
              $count = 0
              foreach ($test in $failedTests) {
                if ($count -ge 10) { break }
                $name = $test.GetAttribute("name")
                $class = $test.GetAttribute("classname")
                $failureMsg = ""
                $failureNode = $test.SelectSingleNode("failure")
                $errorNode = $test.SelectSingleNode("error")
                if ($failureNode) { 
                  $failureMsg = $failureNode.GetAttribute("message")
                  if (-not $failureMsg) { $failureMsg = $failureNode.InnerText }
                } 
                elseif ($errorNode) { 
                  $failureMsg = $errorNode.GetAttribute("message")
                  if (-not $failureMsg) { $failureMsg = $errorNode.InnerText }
                }
                $failureShort = if ($failureMsg.Length -gt 200) { $failureMsg.Substring(0, 200) + "..." } else { $failureMsg }
                $summary += "- **$class::$name**`n"
                $summary += "  ```$failureShort````n`n"
                $count++
              }
              if ($failedTests.Count -gt 10) {
                $summary += "*... and $($failedTests.Count - 10) more failures*`n`n"
              }
            }
            
            Set-Content -Path $summaryPath -Value $summary -Encoding UTF8
            Write-Host "[OK] GitHub Actions summary generated"
          } else {
            Set-Content -Path $summaryPath -Value "## Warning: No test results found`n`nTest execution may have failed before generating reports." -Encoding UTF8
          }

      - name: Generate console summary
        if: always()
        shell: powershell
        run: |
          Write-Host "`n========================================" -ForegroundColor Cyan
          Write-Host "TEST EXECUTION SUMMARY" -ForegroundColor Cyan
          Write-Host "========================================" -ForegroundColor Cyan
          if (Test-Path "reports/junit-smoke.xml") {
            $xml = [xml](Get-Content "reports/junit-smoke.xml")
            
            # Handle both testsuites and testsuite structures
            if ($xml.testsuites) {
              $total = 0
              $failures = 0
              $errors = 0
              $skipped = 0
              $time = 0.0
              foreach ($s in $xml.testsuites.testsuite) {
                $total += [int]$s.tests
                $failures += [int]$s.failures
                $errors += [int]$s.errors
                $skipped += [int]$s.skipped
                $time += [double]$s.time
              }
            } elseif ($xml.testsuite) {
              $total = [int]$xml.testsuite.tests
              $failures = [int]$xml.testsuite.failures
              $errors = [int]$xml.testsuite.errors
              $skipped = [int]$xml.testsuite.skipped
              $time = [double]$xml.testsuite.time
            } else {
              $total = [int]$xml.DocumentElement.GetAttribute("tests")
              $failures = [int]$xml.DocumentElement.GetAttribute("failures")
              $errors = [int]$xml.DocumentElement.GetAttribute("errors")
              $skipped = [int]$xml.DocumentElement.GetAttribute("skipped")
              $time = [double]$xml.DocumentElement.GetAttribute("time")
            }
            
            $passed = $total - $failures - $errors - $skipped
            Write-Host "Total Tests: $total" -ForegroundColor White
            Write-Host "Passed: $passed" -ForegroundColor Green
            Write-Host "Failed: $failures" -ForegroundColor $(if ($failures -gt 0) { "Red" } else { "Green" })
            Write-Host "Errors: $errors" -ForegroundColor $(if ($errors -gt 0) { "Red" } else { "Green" })
            Write-Host "Duration: $time seconds" -ForegroundColor White
            Write-Host "========================================`n" -ForegroundColor Cyan
          }

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: smoke-test-reports
          path: |
            reports/junit-*.xml
            reports/*.html
            reports/*.json
          retention-days: 30

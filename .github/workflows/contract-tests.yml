name: Contract Tests

on:
  push:
    branches: [ main, develop, "**/feature/**", "**/chore/**" ]
  pull_request:
  workflow_dispatch:
    inputs:
      require_server:
        description: "Fail if Focus server is unreachable"
        default: "true"
        type: choice
        options: [ "true", "false" ]

env:
  PYTHON_VERSION: '3.11'

jobs:
  contract:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    env:
      FOCUS_SERVER_HOST: ${{ secrets.FOCUS_SERVER_HOST || '10.10.10.150' }}
      FOCUS_SERVER_PORT: ${{ secrets.FOCUS_SERVER_PORT || '30443' }}
      FOCUS_API_PREFIX: ${{ secrets.FOCUS_API_PREFIX || '/focus-server' }}
      VERIFY_SSL: ${{ secrets.VERIFY_SSL || 'false' }}
      REQUIRE_SERVER: ${{ inputs.require_server || secrets.REQUIRE_SERVER || 'false' }}
      PYTHONDONTWRITEBYTECODE: 1
      PYTHONUNBUFFERED: 1

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install -U pip setuptools wheel
          
          echo "Installing dependencies in stages to avoid resolution conflicts..."
          
          # Stage 1: Core pytest packages (required)
          echo "Stage 1: Installing core pytest packages..."
          pip install --no-cache-dir --prefer-binary \
            pytest==8.2.0 \
            pytest-asyncio==0.23.0 \
            pytest-timeout==2.3.0 \
            pytest-mock==3.14.0 \
            pytest-html==4.1.1 \
            pytest-cov==5.0.0 \
            pytest-json-report==1.5.0 \
            pytest-xdist==3.6.0
          if [ $? -ne 0 ]; then
            echo "ERROR: Stage 1 failed - core pytest packages installation failed"
            exit 1
          fi
          
          # Stage 2: HTTP and data processing (required)
          echo "Stage 2: Installing HTTP and data processing packages..."
          pip install --no-cache-dir --prefer-binary \
            requests==2.32.3 \
            httpx==0.27.0 \
            beautifulsoup4==4.12.3 \
            pydantic==2.9.0 \
            pydantic-settings==2.5.0 \
            orjson==3.10.0 \
            pyyaml==6.0.1
          if [ $? -ne 0 ]; then
            echo "ERROR: Stage 2 failed - HTTP packages installation failed"
            exit 1
          fi
          
          # Stage 3: Infrastructure and utilities (required)
          echo "Stage 3: Installing infrastructure packages..."
          pip install --no-cache-dir --prefer-binary \
            kubernetes==30.1.0 \
            pymongo==4.10.0 \
            paramiko==3.5.0 \
            pika==1.3.2 \
            structlog==24.4.0 \
            colorlog==6.8.2 \
            python-dateutil==2.9.0 \
            pytz==2024.1 \
            psutil==5.9.8 \
            python-dotenv==1.0.1 \
            netaddr==0.10.1 \
            jira==3.10.5
          if [ $? -ne 0 ]; then
            echo "ERROR: Stage 3 failed - infrastructure packages installation failed"
            exit 1
          fi
          
          # Stage 4: Reporting and optional packages (optional - allow failure)
          echo "Stage 4: Installing reporting packages (optional)..."
          pip install --no-cache-dir --prefer-binary \
            allure-pytest==2.13.2 \
            jinja2==3.1.4 \
            asyncio-mqtt==0.16.2 \
            aiofiles==24.1.0 || echo "WARNING: Some reporting packages failed (continuing)..."
          pip install --no-cache-dir --prefer-binary pytest-xray==0.2.1 || echo "WARNING: pytest-xray failed (continuing)..."
          
          echo "Dependency installation completed!"
          echo "Installed packages:"
          pip list | head -30

      - name: Preflight – check Focus availability
        id: preflight
        shell: bash
        run: |
          BASE="https://${FOCUS_SERVER_HOST}:${FOCUS_SERVER_PORT}${FOCUS_API_PREFIX}"
          echo "Checking $BASE/channels"
          set +e
          code=$(curl -sk -o /dev/null -w "%{http_code}" "$BASE/channels")
          echo "status=$code" >> $GITHUB_OUTPUT
          if [[ "$code" -ge 200 && "$code" -lt 400 ]]; then
            echo "reachable=true" >> $GITHUB_OUTPUT
            exit 0
          else
            echo "reachable=false" >> $GITHUB_OUTPUT
            if [[ "${REQUIRE_SERVER}" == "true" ]]; then
              echo "::error::Focus server unreachable (HTTP $code)"
              exit 1
            fi
            exit 0
          fi

      - name: Debug curl (unreachable)
        if: ${{ steps.preflight.outputs.reachable != 'true' }}
        shell: bash
        run: |
          BASE="https://${FOCUS_SERVER_HOST}:${FOCUS_SERVER_PORT}${FOCUS_API_PREFIX}"
          echo "Diagnostic curl -vk $BASE/channels"
          curl -vk "$BASE/channels" || true

      - name: Run contract tests
        if: ${{ steps.preflight.outputs.reachable == 'true' }}
        run: |
          echo "FOCUS: $FOCUS_SERVER_HOST:$FOCUS_SERVER_PORT$FOCUS_API_PREFIX  VERIFY_SSL=$VERIFY_SSL"
          mkdir -p reports
          pytest -q focus_server_api_load_tests/focus_api_tests/test_api_contract.py \
            --junitxml=reports/junit.xml

      - name: Upload test report (junit)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: junit-report
          path: reports/junit.xml

